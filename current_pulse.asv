% izero current pulses
% adds 1 pulse of current to the neuron model at the designated time

function i=current_pulse(t)
global ip;
    if((10<t)&&(t<20))
        i=ip;
    elseif((30<t)&&(t<40))
        i=ip;
    elseif((50<t)&&(t<60))
        i=ip;
    else
        i=0;
    end
end